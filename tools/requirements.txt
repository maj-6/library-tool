# Kept in sync with pyproject.toml [project.dependencies] — pyproject is
# canonical (it also declares the ocr/aws/sheets/capture feature extras).
openpyxl>=3.1
Pillow>=10.0
Flask>=3.0
pypdf>=5.0
PyMuPDF>=1.24
